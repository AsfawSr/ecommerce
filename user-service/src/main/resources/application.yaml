server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: user-service

  # MySQL Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_users?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Asfawpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  # JPA Configuration for Spring Boot 4.0.0 + Hibernate 7.x
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

#  # Security (Basic)
#  security:
#    user:
#      name: admin
#      password: admin123
#      roles: ADMIN


# Logging
logging:
  level:
    com.ecommerce.userservice: DEBUG
    org.hibernate.SQL: DEBUG