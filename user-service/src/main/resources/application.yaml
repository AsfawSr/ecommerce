server:
  port: 8081
  servlet:
    context-path: /api  # Important: Same as Product Service expects!

spring:
  application:
    name: user-service

# Logging
logging:
  level:
    com.ecommerce.userservice: DEBUG